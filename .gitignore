# Vercel
.vercel

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Node / herramienta local de iconos (no se publica)
node_modules
make-icons.mjs
package.json
package-lock.json

# Config y datos locales privados — los usa la tarea local del resumen
# semanal. Contienen tu email y tus cifras. NO se versionan ni se publican.
portfolio.json
snapshots.json
